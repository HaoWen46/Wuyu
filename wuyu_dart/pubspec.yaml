name: wuyu_dart
description: Wuyu protocol layer — JSONL codec, transport interface, and session for Codex App Server.
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  dartssh2: ^2.13.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
